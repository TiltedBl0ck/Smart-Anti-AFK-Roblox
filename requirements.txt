--- Core OS Dependencies (Auto-selected based on running system) ---

Linux-only dependency (provides low-level keyboard input)

evdev>=1.7.0; platform_system == "Linux"

Windows-only dependencies (window focus & keyboard simulation)

pygetwindow>=0.0.9; platform_system == "Windows"
pynput>=1.7.6; platform_system == "Windows"

--- Build/Packaging Dependencies ---

Used to compile your Python code into a standalone .exe or Linux executable

pyinstaller>=6.3.0